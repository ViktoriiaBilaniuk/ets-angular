import { InputMaterialDirective } from './input-material.directive';

describe('InputMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
